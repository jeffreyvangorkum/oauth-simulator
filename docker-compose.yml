services:
  app:
    build: .
    container_name: oauth-simulator
    ports:
      - "3000:3000"
    environment:
      - APP_PASSWORD=admin
      - NODE_ENV=production
    volumes:
      - ./clients.json:/app/clients.json
      - ./data:/app/data
    restart: unless-stopped
